version: 1
applications:
  - appRoot: usdx
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - cd frontend && npm run build
      artifacts:
        baseDirectory: frontend
        files:
          - '.next/**/*'
          - '../node_modules/**/*'
          - 'package.json'
          - 'package-lock.json'
          - 'next.config.js'
          - 'public/**/*'
      cache:
        paths:
          - 'node_modules/**/*'
          - 'frontend/.next/cache/**/*'
